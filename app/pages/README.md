# App Pages

이 폴더는 Streamlit page module을 담습니다.

현재 1단계 전환에서는 기존 page 파일명을 유지합니다.
다음 단계에서 `mig_monitor.py -> migration_monitor_page.py`, `correct_sql.py -> user_edited_sql_page.py`처럼 화면 파일명을 명시적으로 바꿉니다.

