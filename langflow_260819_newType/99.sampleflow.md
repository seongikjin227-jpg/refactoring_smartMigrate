# 001 Sample Flow History

## 분기 테스트 리스트

| No | 분기 테스트 유형 | User Input Test |
|---:|---|---|
| 1 | 일반 대화: 시스템이 수행 가능한 업무 범위를 설명한다. | `너는 뭐 할 수 있어?` |
| 2 | 일반 대화: migration 개념 질문은 작업 실행 없이 답변한다. | `Migration이 뭐야?` |
| 3 | Dashboard 조회: DB Migration 작업 대상 count/progress를 조회한다. | `DB Migration 작업 남은거 있어? 작업 대상 조회해줘` |
| 4 | Dashboard 조회: SQL Conversion 작업 대상 count/progress를 조회한다. | `SQL Conversion 작업 대상 조회해줘` |
| 5 | Dashboard 조회: SQL Tuning 작업 대상은 `STATUS_TUNING IS NULL` and `STATUS_CONVERSION = PASS-CONVERSION` 기준으로 집계한다. | `SQL Tuning 작업 대상 몇 건이야?` |
| 6 | Dashboard 조회: SQL Formatting 작업 대상 count/progress를 조회한다. | `Formatting 대기 작업 몇 건이야?` |
| 7 | 관리 기능: 단건 실행 전 우선순위 변경 요청은 실행 파이프라인이 아니라 상태 변경으로 처리한다. | `map_id=101 priority를 1로 바꿔줘` |
| 8 | 관리 기능: 작업 대상 활성화 요청은 실행 파이프라인이 아니라 상태 변경으로 처리한다. | `map_id=101 USE_YN을 Y로 바꿔줘` |
| 9 | 관리 기능: 사용자가 수정 SQL을 제공하면 correct SQL 입력으로 처리한다. | `map_id = 102의 MIG SQL가 잘못됐어. correct sql으로 select * from emp 로 저장해줘` |
| 10 | 실행 라우팅 예외: 업무 유형이 불명확하면 실행하지 않고 다시 요청하도록 안내한다. | `작업 실행` |
| 11 | 전체 실행: 업무 유형을 명시하지 않은 전체 실행은 pending 우선순위 기준으로 실행 대상을 결정한다. | `대기 작업 전체 실행해줘` |
| 12 | DB Migration 전체 실행: pending MIG 전체를 실행 대상으로 계획한다. | `DB Migration 전체 진행해줘` |
| 13 | 선행 업무 필요: SQL Conversion 요청 시 DB Migration pending이 남아 있으면 `prerequisite_required` 메시지를 반환한다. | `SQL Conversion 전체 실행해줘` |
| 14 | 선행 업무 필요: SQL Tuning 요청 시 DB Migration 또는 SQL Conversion pending이 남아 있으면 `prerequisite_required` 메시지를 반환한다. | `SQL Tuning 전체 진행해줘` |
| 15 | 선행 업무 필요: SQL Formatting 요청 시 앞 단계 pending이 남아 있으면 `prerequisite_required` 메시지를 반환한다. | `SQL Formatting 전체 진행해줘` |
| 16 | 단건 실행: 사용자가 요청한 `map_id`가 MIG 작업 대상이면 해당 1건만 실행 대상으로 계획한다. | `map_id=101 실행해줘` |
| 17 | 복수 실행: 사용자가 요청한 여러 `map_id`가 MIG 작업 대상이면 해당 건들을 순차 실행 대상으로 계획한다. | `map_id=101,102,103 실행해줘` |
| 18 | 단건 실행 대상 미존재: 요청한 `sql_id`가 SQL Conversion 작업 대상이 아니면 `NO_RUNNABLE_JOB` 메시지를 반환한다. | `sql_id=Q001 변환해줘` |
| 19 | 복수 SQL 실행: 요청한 `space_nm` 대상이 SQL Tuning 작업 대상이면 해당 조합들을 순차 실행 대상으로 계획한다. | `space_nm=SALES,HR 튜닝 진행해줘` |
| 20 | 복수 SQL 실행 대상 미존재: 요청한 `space_nm` 대상이 SQL Formatting 작업 대상이 아니면 `NO_RUNNABLE_JOB` 메시지를 반환한다. | `space_nm=SALES,HR 포맷팅 진행해줘` |

이 문서는 예상 사용자 요청 20개를 기준으로 `payload`가 어느 컴포넌트에서 생성되고, 어떤 key가 추가/변경되고, 어떤 output으로 분기되는지 기록한 sample history다.

## Output 종류

| 구분 | 개수 | Output |
|---|---:|---|
| 일반 챗봇 | 1 | `GENERAL_CHAT` |
| Management | 4 | `DASHBOARD`, `STATUS_CHANGE`, `CORRECT_SQL_INPUT`, `EXCEPTION` |
| Execution | 10 | 전체 작업 요청 6개, 단건/복수 작업 요청 4개 |

## 공통 Payload Shape

### 01 Request Classifier LLM 이후

```json
{
  "user_request": "...",
  "route": "GENERAL_CHAT|MANAGEMENT|JOB_EXECUTION",
  "reason": "짧은 한국어 사유"
}
```

### 02 Intent Conditional Router 이후

```json
{
  "...": "...",
  "component": "02_intentRouter",
  "selected_output": "general_chat|management|job_execution",
  "next_node": "03_llmResponse|04_managementRouter|06_getPendingJobs",
  "history": [
    "...",
    {
      "step": "intent_router",
      "message": "route=..."
    }
  ]
}
```

### 04 Management LLM Router 이후

```json
{
  "...": "...",
  "component": "04_managementRouter",
  "management_route": "DASHBOARD|STATUS_CHANGE|CORRECT_SQL_INPUT|EXCEPTION",
  "management_intent": "...",
  "reason": "...",
  "selected_output": "dashboard|status_change|correct_sql_input|exception",
  "next_node": "04_dashboard|04_statusChange|04_correctSqlInput|chat_output",
  "history": [
    "...",
    {
      "step": "management_route",
      "message": "management_route=..."
    }
  ]
}
```

### 06 Get Pending Jobs 이후

`06`은 실행 요청에서만 호출한다. CLOB/SQL 본문, status, retry 정보는 반환하지 않는다. 라우팅에 필요한 count, 대상 식별자, `priority`, `prior_map_id` 같은 경량 메타데이터만 반환한다.

```json
{
  "...": "...",
  "component": "06_getPendingJobs",
  "pending_jobs": {
    "migration_jobs": [
      {
        "job_route": "MIG",
        "job_type": "MIG",
        "map_id": 101,
        "priority": 1,
        "prior_map_id": null
      }
    ],
    "sql_conversion_jobs": [
      {
        "job_route": "SQL_CONVERSION",
        "job_type": "SQL",
        "space_nm": "SALES",
        "sql_id": "Q001",
        "priority": 1
      }
    ],
    "sql_tuning_jobs": [
      {
        "job_route": "SQL_TUNING",
        "job_type": "SQL",
        "space_nm": "SALES",
        "sql_id": "Q002",
        "priority": 1
      }
    ],
    "sql_formatting_jobs": [
      {
        "job_route": "SQL_FORMATTING",
        "job_type": "SQL",
        "space_nm": "SALES",
        "sql_id": "Q003",
        "priority": 1
      }
    ],
    "job_lookup_jobs": [
      "same identifier and routing metadata objects as above"
    ]
  },
  "pending_summary": {
    "total": 4,
    "migration_total": 1,
    "sql_conversion_total": 1,
    "sql_tuning_total": 1,
    "sql_formatting_total": 1
  },
  "next_node": "08_jobExecutionRouter",
  "history": [
    "...",
    {
      "step": "get_pending_jobs",
      "message": "pending jobs loaded"
    }
  ]
}
```

### 08 Job Target Router 이후

실행 가능한 대상이면 `Data` payload로 `09 Execution Plan Summary`에 연결한다.

```json
{
  "...": "...",
  "component": "08_jobExecutionRouter",
  "job_route": "MIG|SQL_CONVERSION|SQL_TUNING|SQL_FORMATTING",
  "run_mode": "all_pending|targeted",
  "run_all_pending": true,
  "target_filter": {
    "map_ids": [],
    "sql_ids": [],
    "space_nms": []
  },
  "selected_jobs": [],
  "routing_reason": "...",
  "selected_output": "mig_job|sql_conversion_job|sql_tuning_job|sql_formatting_job",
  "next_node": "09_executionPlanSummary",
  "history": [
    "...",
    {
      "step": "job_target_route",
      "message": "job_route=..., run_mode=..., count=..."
    }
  ]
}
```

선행 작업이 남아 있으면 `Message`로 바로 Chat Output에 연결한다.

```json
{
  "component": "08_jobExecutionRouter",
  "job_route": "PREREQUISITE_REQUIRED",
  "run_mode": "none",
  "target_filter": {},
  "routing_reason": "DB Migration 작업 대상이 남아 있습니다. 해당 작업을 먼저 진행해주세요.",
  "selected_output": "prerequisite_required",
  "answer_text": "DB Migration 작업 대상이 남아 있습니다. 해당 작업을 먼저 진행해주세요.",
  "final": true,
  "next_node": "chat_output"
}
```

실행 대상이 없으면 `Message`로 바로 Chat Output에 연결한다.

```json
{
  "component": "08_jobExecutionRouter",
  "job_route": "NO_RUNNABLE_JOB",
  "run_mode": "none",
  "target_filter": {},
  "selected_output": "no_runnable_job",
  "answer_text": "사용자에게 바로 보여줄 메시지",
  "final": true,
  "next_node": "chat_output"
}
```

### 09 Execution Plan Summary 이후

`Notice Message`는 Chat Output으로 먼저 연결하고, `Payload`는 선택된 pipeline으로 연결한다.

```json
{
  "...": "...",
  "component": "09_executionPlanSummary",
  "planned_job_route": "MIG|SQL_CONVERSION|SQL_TUNING|SQL_FORMATTING",
  "planned_jobs": [],
  "execution_plan_message": "실행 예정 안내 메시지",
  "execution_plan_prompt": "LLM 응답용 프롬프트",
  "next_node": "10_migPipeline|12_sqlConversionPipeline|15_sqlTuningPipeline|17_sqlFormattingPipeline",
  "history": [
    "...",
    {
      "step": "execution_plan_summary",
      "message": "planned ... job(s)"
    }
  ]
}
```

### Pipeline 이후

```json
{
  "...": "...",
  "component": "10_migPipeline|12_sqlConversionPipeline|15_sqlTuningPipeline|17_sqlFormattingPipeline",
  "pipeline_status": "SUCCESS-TEST|PARTIAL_FAIL-TEST|FAIL-TEST",
  "job_result": {
    "status": "...",
    "run_mode": "all_pending|targeted",
    "processed_jobs": []
  },
  "next_node": "13_finalSummary"
}
```

### 13 Final Summary 이후

```json
{
  "...": "...",
  "component": "13_finalSummary",
  "answer_text": "최종 요약 메시지",
  "final": true
}
```

---

# Sample Requests

## 1. 일반 챗봇

### 요청

```text
이 플로우 구조 간단히 설명해줘
```

### Payload History

| Step | Component | Output | Payload 변경 |
|---:|---|---|---|
| 1 | `01 Request Classifier LLM` | `Message(JSON text)` | `route=GENERAL_CHAT`, `user_request`, `reason` |
| 2 | `02 Intent Conditional Router` | `General Chat` | `selected_output=general_chat`, `next_node=03_llmResponse` |
| 3 | `03 LLM Response` | `LLM Message` | `03_llmResponsePrompt.md`와 `payload.user_request`를 LLM에 입력해 답변 생성 |

### 최종 연결

```text
Chat Input -> 01 -> 02.General Chat -> 03 -> Chat Output
```

---

## 2. Management - Dashboard 작업 대상 조회

### 요청

```text
SQL Conversion 작업 대상 조회해줘
```

### Payload History

| Step | Component | Output | Payload 변경 |
|---:|---|---|---|
| 1 | `01 Request Classifier LLM` | `Message(JSON text)` | `route=MANAGEMENT`, `user_request`, `reason` |
| 2 | `02 Intent Conditional Router` | `Management` | `selected_output=management`, `next_node=04_managementRouter` |
| 3 | `04 Management LLM Router` | `Dashboard` | `management_route=DASHBOARD`, `selected_output=dashboard`, `next_node=04_dashboard` |
| 4 | `04 Dashboard` | `Result Message` | `dashboard_data/query_result`가 있으면 그 내용을 포함해 `answer_text` 생성 |

### 최종 연결

```text
Chat Input -> 01 -> 02.Management -> 04.Dashboard -> 04_dashboard -> Chat Output
```

---

## 3. Management - Status Change

### 요청

```text
map_id=101 priority를 1로 바꿔줘
```

### Payload History

| Step | Component | Output | Payload 변경 |
|---:|---|---|---|
| 1 | `01 Request Classifier LLM` | `Message(JSON text)` | `route=MANAGEMENT`, `user_request`, `reason` |
| 2 | `02 Intent Conditional Router` | `Management` | `next_node=04_managementRouter` |
| 3 | `04 Management LLM Router` | `Status Change` | `management_route=STATUS_CHANGE`, `selected_output=status_change`, `next_node=04_statusChange` |
| 4 | `04 Status Change` | `Result Message` | 상태/priority 변경 요청 메시지 생성 |

### 최종 연결

```text
Chat Input -> 01 -> 02.Management -> 04.Status Change -> 04_statusChange -> Chat Output
```

---

## 4. Management - Correct SQL Input

### 요청

```text
sql_id=Q001 correct sql은 select * from emp 로 저장해줘
```

### Payload History

| Step | Component | Output | Payload 변경 |
|---:|---|---|---|
| 1 | `01 Request Classifier LLM` | `Message(JSON text)` | `route=MANAGEMENT`, `user_request`, `reason` |
| 2 | `02 Intent Conditional Router` | `Management` | `next_node=04_managementRouter` |
| 3 | `04 Management LLM Router` | `Correct SQL Input` | `management_route=CORRECT_SQL_INPUT`, `selected_output=correct_sql_input`, `next_node=04_correctSqlInput` |
| 4 | `04 Correct SQL Input` | `Result Message` | Correct SQL 입력 요청 메시지 생성 |

### 최종 연결

```text
Chat Input -> 01 -> 02.Management -> 04.Correct SQL Input -> 04_correctSqlInput -> Chat Output
```

---

## 5. Management - Exception

### 요청

```text
저거 적당히 처리해줘
```

### Payload History

| Step | Component | Output | Payload 변경 |
|---:|---|---|---|
| 1 | `01 Request Classifier LLM` | `Message(JSON text)` | `route=MANAGEMENT`로 판단될 수 있으나 세부 관리 의도가 불명확 |
| 2 | `02 Intent Conditional Router` | `Management` | `next_node=04_managementRouter` |
| 3 | `04 Management LLM Router` | `Exception Message` | `management_route=EXCEPTION`, `selected_output=exception`, `answer_text` 생성, `final=true` |

### 최종 연결

```text
Chat Input -> 01 -> 02.Management -> 04.Exception Message -> Chat Output
```

---

# Execution - 전체 작업 요청 6개

## 6. 전체 Pending 작업 실행

### 요청

```text
대기 작업 전체 실행해줘
```

### Payload History

| Step | Component | Output | Payload 변경 |
|---:|---|---|---|
| 1 | `01 Request Classifier LLM` | `Message(JSON text)` | `route=JOB_EXECUTION`, `user_request`, `reason` |
| 2 | `02 Intent Conditional Router` | `Job Execution` | `selected_output=job_execution`, `next_node=06_getPendingJobs` |
| 3 | `06 Get Pending Jobs` | `payload` | `pending_jobs`, `pending_summary` 추가 |
| 4 | `08 Job Target Router` | domain target | LLM이 pending 상태 기준으로 첫 실행 domain 선택, `run_mode=all_pending`, `selected_jobs` 추가 |
| 5 | `09 Execution Plan Summary` | `Notice Message` + `Payload` | 실행 예정 job 수/목록 메시지 생성, pipeline payload 생성 |
| 6 | selected Pipeline | `payload` | `pipeline_status`, `job_result.processed_jobs` 추가 |
| 7 | `13 Final Summary` | `Result Message` | 전체 실행 결과 요약 |

### 최종 연결

```text
Chat Input -> 01 -> 02.Job Execution -> 06 -> 08 -> 09 -> selected Pipeline -> 13 -> Chat Output
```

---

## 7. DB Migration 전체 실행

### 요청

```text
DB Migration 전체 진행해줘
```

### Payload Delta

```json
{
  "route": "JOB_EXECUTION",
  "job_route": "MIG",
  "run_mode": "all_pending",
  "run_all_pending": true,
  "target_filter": {
    "map_ids": [],
    "sql_ids": [],
    "space_nms": []
  },
  "next_node": "10_migPipeline"
}
```

### 최종 연결

```text
Chat Input -> 01 -> 02.Job Execution -> 06 -> 08.MIG Targets -> 09 -> 10_migPipeline -> 13 -> Chat Output
```

---

## 8. SQL Conversion 전체 실행

### 요청

```text
SQL Conversion 전체 실행해줘
```

### Payload Delta

```json
{
  "route": "JOB_EXECUTION",
  "job_route": "SQL_CONVERSION",
  "run_mode": "all_pending",
  "run_all_pending": true,
  "target_filter": {
    "map_ids": [],
    "sql_ids": [],
    "space_nms": []
  },
  "next_node": "12_sqlConversionPipeline"
}
```

### 최종 연결

```text
Chat Input -> 01 -> 02.Job Execution -> 06 -> 08.SQL Conversion Targets -> 09 -> 12_sqlConversionPipeline -> 13 -> Chat Output
```

---

## 9. SQL Tuning 전체 실행

### 요청

```text
SQL Tuning 전체 진행해줘
```

### Payload Delta

```json
{
  "route": "JOB_EXECUTION",
  "job_route": "SQL_TUNING",
  "run_mode": "all_pending",
  "run_all_pending": true,
  "target_filter": {
    "map_ids": [],
    "sql_ids": [],
    "space_nms": []
  },
  "next_node": "15_sqlTuningPipeline"
}
```

### 최종 연결

```text
Chat Input -> 01 -> 02.Job Execution -> 06 -> 08.SQL Tuning Targets -> 09 -> 15_sqlTuningPipeline -> 13 -> Chat Output
```

---

## 10. SQL Formatting 전체 실행

### 요청

```text
SQL Formatting 전체 진행해줘
```

### Payload Delta

```json
{
  "route": "JOB_EXECUTION",
  "job_route": "SQL_FORMATTING",
  "run_mode": "all_pending",
  "run_all_pending": true,
  "target_filter": {
    "map_ids": [],
    "sql_ids": [],
    "space_nms": []
  },
  "next_node": "17_sqlFormattingPipeline"
}
```

### 최종 연결

```text
Chat Input -> 01 -> 02.Job Execution -> 06 -> 08.SQL Formatting Targets -> 09 -> 17_sqlFormattingPipeline -> 13 -> Chat Output
```

---

## 11. SQL 계열 전체 실행

### 요청

```text
SQL 관련 작업 전체 실행해줘
```

### Payload History

| Step | Component | Output | Payload 변경 |
|---:|---|---|---|
| 1 | `01 Request Classifier LLM` | `Message(JSON text)` | `route=JOB_EXECUTION`, `user_request`, `reason` |
| 2 | `06 Get Pending Jobs` | `payload` | SQL Conversion/Tuning/Formatting pending 현황 추가 |
| 3 | `08 Job Target Router` | domain target | LLM이 요청과 pending 상태를 보고 `SQL_CONVERSION`, `SQL_TUNING`, `SQL_FORMATTING` 중 하나를 선택 |
| 4 | `09 Execution Plan Summary` | `Notice Message` + `Payload` | 선택된 domain의 실행 예정 목록 생성 |
| 5 | selected SQL Pipeline | `payload` | 테스트 실행 결과 생성 |
| 6 | `13 Final Summary` | `Result Message` | 최종 요약 |

### 비고

현재 구조에서 하나의 `08` output은 하나의 domain pipeline으로 연결된다. SQL Conversion, Tuning, Formatting을 모두 순차 실행하려면 multi-domain orchestration 컴포넌트를 별도로 추가해야 한다.

---

# Execution - 단건/복수 작업 요청 4개

## 12. DB Migration 단건 실행

### 요청

```text
map_id=101 실행해줘
```

### Payload Delta

```json
{
  "route": "JOB_EXECUTION",
  "job_route": "MIG",
  "run_mode": "targeted",
  "run_all_pending": false,
  "target_filter": {
    "map_ids": [101],
    "sql_ids": [],
    "space_nms": []
  },
  "selected_jobs": [
    {
      "job_route": "MIG",
      "job_type": "MIG",
      "map_id": 101
    }
  ],
  "next_node": "10_migPipeline"
}
```

### 최종 연결

```text
Chat Input -> 01 -> 02.Job Execution -> 06 -> 08.MIG Targets -> 09 -> 10_migPipeline -> 13 -> Chat Output
```

---

## 13. DB Migration 복수건 실행

### 요청

```text
map_id=101,102,103 실행해줘
```

### Payload Delta

```json
{
  "route": "JOB_EXECUTION",
  "job_route": "MIG",
  "run_mode": "targeted",
  "run_all_pending": false,
  "target_filter": {
    "map_ids": [101, 102, 103],
    "sql_ids": [],
    "space_nms": []
  },
  "selected_jobs": [
    { "job_route": "MIG", "job_type": "MIG", "map_id": 101 },
    { "job_route": "MIG", "job_type": "MIG", "map_id": 102 },
    { "job_route": "MIG", "job_type": "MIG", "map_id": 103 }
  ],
  "next_node": "10_migPipeline"
}
```

### 최종 연결

```text
Chat Input -> 01 -> 02.Job Execution -> 06 -> 08.MIG Targets -> 09 -> 10_migPipeline -> 13 -> Chat Output
```

---

## 14. SQL Conversion 단건 실행

### 요청

```text
sql_id=Q001 변환해줘
```

### Payload Delta

```json
{
  "route": "JOB_EXECUTION",
  "job_route": "SQL_CONVERSION",
  "run_mode": "targeted",
  "run_all_pending": false,
  "target_filter": {
    "map_ids": [],
    "sql_ids": ["Q001"],
    "space_nms": []
  },
  "selected_jobs": [
    {
      "job_route": "SQL_CONVERSION",
      "job_type": "SQL",
      "sql_id": "Q001"
    }
  ],
  "next_node": "12_sqlConversionPipeline"
}
```

### 최종 연결

```text
Chat Input -> 01 -> 02.Job Execution -> 06 -> 08.SQL Conversion Targets -> 09 -> 12_sqlConversionPipeline -> 13 -> Chat Output
```

---

## 15. SQL Tuning/Formatting 복수건 실행

### 요청

```text
space_nm=SALES,HR 튜닝 진행해줘
```

### Payload Delta

```json
{
  "route": "JOB_EXECUTION",
  "job_route": "SQL_TUNING",
  "run_mode": "targeted",
  "run_all_pending": false,
  "target_filter": {
    "map_ids": [],
    "sql_ids": [],
    "space_nms": ["SALES", "HR"]
  },
  "selected_jobs": [
    {
      "job_route": "SQL_TUNING",
      "job_type": "SQL",
      "space_nm": "SALES"
    },
    {
      "job_route": "SQL_TUNING",
      "job_type": "SQL",
      "space_nm": "HR"
    }
  ],
  "next_node": "15_sqlTuningPipeline"
}
```

### Formatting 요청일 때 차이

```text
space_nm=SALES,HR 포맷팅 진행해줘
```

이면 `job_route=SQL_FORMATTING`, `next_node=17_sqlFormattingPipeline`로 바뀐다.

### 최종 연결

```text
Chat Input -> 01 -> 02.Job Execution -> 06 -> 08.SQL Tuning Targets -> 09 -> 15_sqlTuningPipeline -> 13 -> Chat Output
```

---

# No Runnable Message 예시

이 케이스는 사용자가 요청한 `map_id` 또는 `sql_id + space_nm` 조합이 `06 Get Pending Jobs`의 작업 대상 목록에서 조회되지 않을 때 반환한다.

## No Runnable Target

### 요청

```text
map_id=101 실행해줘
```

### 조건

```text
06 Get Pending Jobs의 migration_jobs에 map_id=101이 없는 경우
```

### Output

```text
08 Job Target Router.No Runnable Target Message -> Chat Output
```

### Message Payload

```json
{
  "component": "08_jobExecutionRouter",
  "job_route": "NO_RUNNABLE_JOB",
  "run_mode": "none",
  "selected_jobs": [],
  "answer_text": "map_id=101이 작업 대상에서 조회되지 않았습니다...",
  "next_node": "chat_output",
  "final": true
}
```
