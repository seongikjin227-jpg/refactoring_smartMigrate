# XML Utilities

MyBatis mapper XML을 다루는 보조 패키지입니다.

역할:

- mapper XML 파싱
- include 조각 확장
- schema prefix 정리
- DB에 저장된 SQL을 mapper XML 형태로 export

이 패키지는 supervisor workflow가 아니며, 운영자가 UI나 스크립트에서 필요할 때 실행하는 예외/보조 기능입니다.

